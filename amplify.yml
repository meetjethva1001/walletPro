version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npx tsc
  artifacts:
    baseDirectory: ./
    files:
      - index.html
      - style.css
      - index.js
      - '**/*'
  cache:
    paths:
      - node_modules/**/*